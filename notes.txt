few important things from practical
- name of api variable should be uppaer case only
- my langchain version doesn't support model provider backend when creating agent, i need to use init
- tool are very much resrticted to docstring
- instead of provider we can also add name:mode, "google_genai:gemini-2.5-flash"
- apart from init_chat_model, there is specific libraries for each model, e.g openai, gemini etc
- stream, show output while its generation
- batch, multiple prompts in one list, and get their responses in parallel, improves performance and reduce api costs

Rewrite my experience section naturally include those keywords and remove the redflags. Use google x,y,z formula, where z accomplish x as measured by y by doing z

Act as an ATS filter and a hiring manager reading 200 resume in one sitting scan my new resume and tell me which sections would get skipped andrewrite them so that they actually stop scroll
